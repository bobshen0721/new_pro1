# 電話錄音調參指南

## 建議起點

先使用介面的「平衡（建議先用）」：

| 參數 | 建議值 | 白話說明 |
|---|---:|---|
| `threshold` | 0.35 | 聲音多像人聲才開始保留 |
| `min_speech_duration_ms` | 100 | 保留「對、好、嗯」等短回答 |
| `min_silence_duration_ms` | 450 | 靜音約半秒才切成下一段 |
| `speech_pad_ms` | 250 | 語音前後多保留一點，避免吃掉字首字尾 |
| 最短重疊區間 | 0.20 秒 | 太短可能只是模型邊界抖動 |
| 字詞重疊比例 | 0.25 | 一個字至少四分之一落在重疊區才標記 |

## 三組預設

### 平衡

```text
threshold = 0.35
min_speech_duration_ms = 100
min_silence_duration_ms = 450
speech_pad_ms = 250
```

### 雜音較多

```text
threshold = 0.45
min_speech_duration_ms = 130
min_silence_duration_ms = 500
speech_pad_ms = 250
```

雜音文字會減少，但小聲客戶也可能被漏掉。

### 遠端聲音較小

```text
threshold = 0.28
min_speech_duration_ms = 80
min_silence_duration_ms = 400
speech_pad_ms = 300
```

較不容易漏字，但可能把更多背景聲當成人聲。

## 常見問題怎麼調

| 現象 | 優先調整 |
|---|---|
| 客戶聲音小、整句消失 | `threshold` 每次降低 0.03 |
| 雜音被辨識成文字 | `threshold` 每次提高 0.03 |
| 「好、對、嗯」消失 | 最短語音降到 60～80 毫秒 |
| 一句話被切得太碎 | 切段靜音提高到 600～800 毫秒 |
| 兩人輪流說話黏在一起 | 切段靜音降低到 300～400 毫秒 |
| 開頭或句尾缺字 | 前後保留提高到 300～400 毫秒 |
| 疑似重疊標記太多 | 最短重疊提高到 0.35 秒；字詞比例提高到 0.40 |
| 短暫搶話沒有標出 | 最短重疊降到 0.12 秒；字詞比例降到 0.20 |
| 長串重複文字 | 維持 `condition_on_previous_text=False` |

## 不建議先做的事

1. 不要先把 Silero 判定的靜音刪掉再拼接，否則逐字稿與播放器時間會對不上。
2. 不要一開始就用強力降噪，可能連電話中的細小子音一起消失。
3. 不要使用太強的禁止重複規則，日期、金額與代號本來就可能重複。

## 兩個辨識模型怎麼看

- TEA-ASR-1.1-mini 是主逐字稿，Qwen3-ForcedAligner 只負責把主稿對回音檔時間。
- Whisper large-v2 是獨立比對稿，不會自動覆蓋 TEA-ASR。
- 系統利用兩邊的時間戳，把 Whisper 字詞分配到同時間的 TEA 段落；若沒有字詞時間戳才改用 Whisper 段落時間。
- 每段文字會先統一繁簡體、英文大小寫及標點，再計算正規化編輯距離相似度。
- 每段仍採用 TEA 主稿；低於 90% 的段落只會把逐字稿文字改成紅色。
- 頁面上方的總分是依各段文字長度加權後的參考值，人工檢查以紅色文字為準。
- 可在「金融領域提示」說明固定領域，在「金融關鍵詞」加入當期基金名、公司名與英文縮寫；不要放入一大段猜測中的會議內容。

## 改用 mini 後要重新確認的項目

- `TEA-ASR-1.1-mini` 是 780M compact checkpoint；相較原本 2B `TEA-ASR-1.1` 較省模型空間與顯示卡記憶體，但困難的中英夾雜、低音質及專業詞彙可能有不同錯誤分布。
- Silero VAD、Qwen3-ForcedAligner、Pyannote、逐段相似度及點擊跳播的時間基準都沒有因換模型而改變，不要為了換模型而先移動或重組靜音區段。
- 原本 2B 模型測出的門檻與提示詞不能直接視為 mini 的最佳值。請先維持「平衡」預設，再用同一批人工校正電話比較中文字錯誤率、金融關鍵欄位及中英夾雜。
- 畫面上的 90% 是 TEA 與 Whisper 兩份結果的一致度門檻，不是正確率。兩個模型也可能同時辨識錯，因此金額、日期、帳號與證券代號仍要人工核對。
- 不要把舊 `TEA-ASR-1.1` 資料夾直接改名成 `TEA-ASR-1.1-mini`。應用程式會檢查 mini 的必要檔案，但只有從正確模型來源下載才會得到正確權重。

## 兩人同時說話的限制

- Silero VAD 只判斷「有人說話」，不判斷是誰。
- Silero VAD 的切段同時供 TEA-ASR 主稿使用，Whisper 也用相同參數過濾靜音。
- Pyannote Community-1 判斷 A／B 及疑似重疊時間。
- TEA-ASR-1.1-mini 與 Whisper 仍只看到混在一起的單聲道聲音。
- 重疊區可能只保留較大聲的人，或把兩人的內容混成一句。
- 正式法遵或稽核判定時，疑似重疊區應強制人工複核。

## 建議測試方法

至少準備 30 通人工校正過的電話，涵蓋安靜、雜音、小聲客戶、短回答、快速輪流說話、同時說話，以及日期、金額、基金名稱和證券代號。

比較以下指標：

1. 中文字錯誤率。
2. 金額、日期、代號召回率。
3. 短回答漏掉次數。
4. 靜音產生假文字次數。
5. 重疊區漏標與誤標次數。
6. TEA-ASR-1.1-mini 與 Whisper 分歧片段中，各自真正答對的比例。

請使用公司實際的 8 kHz 電話錄音測試。公開的台灣國語排行榜不能直接代表金融客服電話效果。
